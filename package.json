{
  "name": "@openquests/engine",
  "version": "1.0.0",
  "description": "Daily tick engine for GitHub-native D&D",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "bin": {
    "openquests": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "tick": "ts-node src/index.ts",
    "prepare": "npm run build",
    "test": "node --env-file=.env ./node_modules/vitest/vitest.mjs run",
    "vitest": "npx vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@google/genai": "^1.39.0",
    "@openquests/schema": "github:the-real-han/openquests-schema"
  },
  "devDependencies": {
    "@types/node": "^20.19.30",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2",
    "vitest": "^4.0.18"
  }
}